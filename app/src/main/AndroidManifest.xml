<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pramod_shash.lankaweparttime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeScreenActivity"
            android:label="@string/title_activity_welcome_screen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EmployeeLoginActivity"
            android:label="@string/title_activity_employee_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EmployeeRegistrationActivity" />
        <activity android:name=".EmployeeHomeActivity" />
        <activity android:name=".EmployerLoginActivity" />
        <activity android:name=".EmployerRegistrationActivity" />
        <activity android:name=".EmployerHomeActivity"></activity>
    </application>

</manifest>